#include <stdio.h>
/**
 * main - prints the alphabets in small letters
 *
 * Return: returns zero value
 */
int main(void)
{
char alpha = 'a';
for (alpha = 'a'; alpha <= 'z'; alpha++)
putchar(alpha\n);
return (0);
}
